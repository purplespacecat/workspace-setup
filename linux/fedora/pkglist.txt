# Dev/CLI tools only — Fedora KDE already ships the base system + desktop,
# so (unlike the Arch list) this omits anything preinstalled: curl, fontconfig,
# btrfs-progs, flatpak, etc. Cross-checked against linux/ubuntu/pkglist.txt.

# Core utilities
git
unzip
zip
7zip
jq

# Editor / shell / terminal
zsh
vim-enhanced
tmux
direnv

# Secrets (sops is not packaged for Fedora — installed via go in config script)
age

# Dev tools
cmake
luarocks
golang

# Search / file / clipboard
fd-find
ripgrep
wl-clipboard
bat
zoxide

# CLIs
gh

# System info
fastfetch
